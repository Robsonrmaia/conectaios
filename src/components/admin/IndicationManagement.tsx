import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

const IndicationManagement = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Gerenciamento de Indicações</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Sistema de indicações temporariamente simplificado</p>
      </CardContent>
    </Card>
  );
};

export default IndicationManagement;