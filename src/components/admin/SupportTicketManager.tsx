import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

const SupportTicketManager = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Gerenciamento de Tickets</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Sistema de tickets temporariamente simplificado</p>
      </CardContent>
    </Card>
  );
};

export default SupportTicketManager;